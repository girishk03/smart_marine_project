[pytest]
minversion = 6.0
addopts = -ra
# Only run the real pytest suite under ./tests
# (the repo root contains exploratory scripts named test_*.py)
testpaths = tests
python_files = test_*.py
norecursedirs = smp_env venv yolov5 models results static __pycache__
